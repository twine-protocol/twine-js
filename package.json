{
  "name": "@twine-protocol/twine-js",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "watch": "lerna watch -- lerna run build --scope=\\$LERNA_PACKAGE_NAME",
    "version": "lerna version --no-push --no-private --no-commit-hooks",
    "test": "bun run build && bun test"
  },
  "devDependencies": {
    "bun-types": "latest",
    "lerna": "^7.3.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {}
}
